# API
fastapi>=0.115
uvicorn[standard]>=0.34

# Database
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14

# Validation & config
pydantic>=2.10
pydantic-settings>=2.7
email-validator>=2.2

# Auth
PyJWT>=2.10
bcrypt>=4.2

# Infra clients
redis>=5.2
boto3>=1.35

# PDF
reportlab>=4.2

# OCR (kéo theo torch ~2GB disk — cân nhắc image Docker riêng cho OCR khi tách service)
easyocr>=1.7
opencv-python-headless>=4.10
numpy>=1.26

# Upload file (UploadFile cần python-multipart)
python-multipart>=0.0.18

# Recommender (ghép phòng)
scikit-learn>=1.5
